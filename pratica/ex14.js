let animal= ['cachorro','gato', "cobra", "rato", "coelho", "elefante"]
   let pos= animal.indexOf('gato')
    if (pos== -1){
        console.log('[FALSE]O valor não foi declarado')
    }else{
console.log(`[TRUE] O Valor é verdadeiro, está na posição ${pos}`)
    }
   
    /*let animais = ["leão", "elefante", "girafa", "tigre", "hipopótamo"];

        function pesquisarAnimal(nomeAnimal) {
        return animais.includes(nomeAnimal);
        }

        // Exemplo de uso:
        let resultadoPesquisa = pesquisarAnimal("girafa");
        console.log("Animal encontrado?", resultadoPesquisa);
        */ 